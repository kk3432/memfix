# memfix v1.1.0 发布说明

**发布日期**: 2026-09-12
**目标设备**: 小米AX3000T (联发科MT7981B版)
**架构**: aarch64 (ARM64) 静态二进制

---

## 概述

v1.1.0 是一个功能增强版本，新增了场景调控（密集监控）、单次强制回收、日志自动轮换和统计信息功能，并修复了时间戳时区显示错误的问题。

---

## 新功能

### 1. 场景调控（密集监控模式）

触发内存回收后，程序自动进入**5分钟密集监控期**，期间每**2秒**检测一次内存状态（正常模式为每30秒）。

**设计目的**: 内存泄漏往往在回收后短时间内再次发生。密集监控能更快捕捉回收后的内存变化趋势，及时发现二次泄漏并再次回收，避免内存快速耗尽导致路由器卡顿。

**工作流程**:
```
正常监控(30秒间隔) → 触发回收 → 密集监控(2秒间隔, 持续5分钟) → 恢复正常监控
```

**状态查看**: `memfix status` 会显示当前是否处于密集监控模式及剩余时间。

### 2. 单次强制回收

`memfix once` 命令现在**不论当前内存是否充足**，都强制执行完整回收流程：
- drop_caches (释放页缓存+目录项+inode)
- compact_memory (压缩Slab)
- 可选的泄漏进程重启（如果配置了）

**使用场景**:
- 手动维护路由器时主动清理内存
- 测试回收功能是否正常工作
- 回收后观察内存变化趋势

### 3. 日志自动轮换

日志文件每**3天**自动轮换一次：
- 当前日志: `/var/log/memfix.log`
- 轮换后旧日志: `/var/log/memfix.log.1`
- 下次轮换时覆盖 `.1` 文件

**目的**: 路由器存储空间有限（AX3000T可写分区约24MB），避免日志文件无限增长占用存储空间。

### 4. status 统计信息

`memfix status` 命令新增**近三天统计面板**：
- 回收次数: 近三天内执行的内存回收总次数
- 平均可用内存: 近三天日志采样的平均可用内存百分比
- 密集监控状态: 当前是否处于密集监控模式及剩余时间

统计数据基于日志文件（含 `.log.1` 轮换日志）解析，自动过滤三天前的记录。

---

## 修复

### 时间戳时区bug（严重）

**问题**: 路由器系统时区为UTC时，日志时间戳显示比实际时间晚8小时。例如实际时间19:33，日志显示为11:33。

**根因**: 小米路由器OpenWrt固件的系统时区默认为UTC，`time.Now().Format()` 使用系统时区格式化时间戳，导致时间偏移8小时。

**修复**: 使用固定CST(UTC+8)时区 `time.FixedZone("CST", 8*3600)` 格式化所有时间戳，不依赖系统时区设置。该方式不依赖系统tzdata，在嵌入式系统上可靠工作。

### 日志轮换mtime bug

**问题**: 守护进程启动时以 `O_APPEND` 模式打开日志文件，这会更新文件的mtime为当前时间，导致日志轮换检测永远认为文件是新创建的，不会触发轮换。

**修复**: 在打开日志文件**之前**先执行轮换检查（`rotateLogIfNeeded`），确保mtime检测准确。

---

## 下载资产

| 文件 | 说明 | 大小 |
|------|------|------|
| `memfix-1.1.0-aarch64.tar.gz` | 完整发布包（二进制+配置+脚本+文档+源码） | ~800KB |
| `memfix-1.1.0-src.tar.gz` | 纯源码包（C+Go双版本源码+构建系统） | ~25KB |
| `memfix` | aarch64静态二进制（单独） | ~1.9MB |

## SHA256 校验和

```
memfix (aarch64): a2c23c79ff1538eb455d3bb3bf10ae35d1fd9fe027c4e59733eaab70324bf8e4
```

---

## 升级指南

### 从 v1.0.1 升级

```bash
# 1. 停止旧版本
/userdisk/.memfix/memfixctl stop

# 2. 备份旧配置
cp /userdisk/.memfix/memfix.conf /userdisk/.memfix/memfix.conf.bak

# 3. 替换二进制
cp memfix /userdisk/.memfix/memfix
chmod +x /userdisk/.memfix/memfix

# 4. 启动新版本
/userdisk/.memfix/memfixctl start

# 5. 验证
/userdisk/.memfix/memfixctl status
```

### 全新安装

参见 README.md 中的安装说明。

---

## 兼容性

- **设备**: 小米AX3000T (联发科MT7981B版)
- **固件**: 小米官方固件（基于OpenWrt 18.06定制）
- **架构**: aarch64 (ARM64, Cortex-A53)
- **依赖**: 无外部依赖，纯静态二进制
- **内存占用**: 运行时RSS ~2.5MB
- **配置兼容**: v1.0.x 配置文件可直接使用，无需修改

---

## 技术细节

- **语言**: Go 1.23.0
- **编译**: `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -ldflags="-s -w"`
- **链接**: 纯静态，无动态库依赖
- **二进制大小**: ~1.9MB (stripped)
- **密集监控参数**: 持续300秒(5分钟)，间隔2秒
- **日志轮换周期**: 3天
- **时区**: 固定CST(UTC+8)，不依赖系统时区

---

## 已知限制

- C版本musl静态二进制因musl.cc工具链下载不稳定，CI中可能跳过构建（gnu版本和Go版本不受影响）
- 日志统计基于DEBUG级别的内存状态采样，如果关闭verbose模式，平均可用内存统计可能无数据
- 密集监控模式在程序重启后不会自动恢复（需要再次触发回收才会进入）

---

## 反馈

- GitHub Issues: https://github.com/kk3432/memfix/issues
- 提交bug时请附上 `memfix status` 输出和最近的日志内容

---

## 许可证

MIT License - 详见 LICENSE 文件
